language: r

sudo: required

warnings_are_errors: true

cache: packages


apt_packages:
  - openjdk-8-jre
  - r-cran-rjava

before_install:
  - sudo $(which R) CMD javareconf


matrix:
  include:
    - os: linux
      r: release


r_packages:
  - covr
  - rJava

after_success:
  - R -q -e 'library("covr");codecov()'

